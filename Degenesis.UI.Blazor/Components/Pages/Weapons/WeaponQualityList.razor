@page "/weapon-qualities"
@using Degenesis.UI.Service.Features.Weapons
@inject WeaponQualityService WeaponQualityService
@inject IDialogService DialogService
@layout Layout.DashBoardLayout

<h3>Weapon Qualities</h3>

<MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="ShowCreateDialog">Create New Weapon Quality</MudButton>

@if (weaponQualities is null)
{
    <p><em>Loading...</em></p>
}
else
{
    <MudTable Items="@weaponQualities" Hover="true">
        <HeaderContent>
            <MudTh>Name</MudTh>
            <MudTh>Description</MudTh>
            <MudTh>Actions</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd>@context.Name</MudTd>
            <MudTd>@context.Description</MudTd>
            <MudTd>
                <MudButton OnClick="() => ShowEditDialog(context.Id)" Color="Color.Primary">Edit</MudButton>
                <MudButton OnClick="() => DeleteWeaponQuality(context.Id)" Color="Color.Error">Delete</MudButton>
            </MudTd>
        </RowTemplate>
    </MudTable>
}