@page "/dashboard/equipments"

<h3>Equipments</h3>

@if (Equipments is null)
{
    <MudProgressCircular Color="Color.Primary" Size="Size.Large" Indeterminate="true" />
}
else
{
    <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="ShowCreateDialog">Create New Equipment</MudButton>
    <MudTable Bordered Items="@Equipments" Hover="true">
        <HeaderContent>
            <MudTh>Name</MudTh>
            <MudTh>Equipment Type</MudTh>
            <MudTh>Description</MudTh>
            <MudTh>Effect</MudTh>
            <MudTh>Encumbrance</MudTh>
            <MudTh>TechLevel</MudTh>
            <MudTh>Slots Taken</MudTh>
            <MudTh>Value</MudTh>
            <MudTh>Resources</MudTh>
            <MudTh>Cults</MudTh>
            <MudTh>Actions</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd>@context.Name</MudTd>
            <MudTd>@context.EquipmentType.Name</MudTd>
                    <MudTd>@(string.IsNullOrEmpty(context.Description) ? "-" : context.Description)</MudTd>
                    <MudTd>@(string.IsNullOrEmpty(context.Effect) ? "-" : context.Effect)</MudTd>
                    <MudTd>@(context.Encumbrance == 0 ? "-" : context.Encumbrance)</MudTd>
                    <MudTd>@(context.TechLevel == 0 ? "-" : context.TechLevel)</MudTd>
                    <MudTd>@(context.SlotsTaken == 0 ? "-" : context.SlotsTaken)</MudTd>
                    <MudTd>@(string.IsNullOrEmpty(context.Value) ? "-" : context.Value)</MudTd>
                    <MudTd>@(string.IsNullOrEmpty(context.Resources) ? "-" : context.Resources)</MudTd>
            <MudTd>
                    @if (context.Cults != null && context.Cults.Any())
                    {
                        @string.Join(", ", context.Cults.Select(bs => bs.Name))
                    }
                    else
                    {
                        <span> - </span>
                    }
            </MudTd>
            <MudTd>
                <MudButton OnClick="() => ShowEditDialog(context.Id)" Color="Color.Primary">Edit</MudButton>
                <MudButton OnClick="() => DeleteEquipment(context.Id)" Color="Color.Error">Delete</MudButton>
            </MudTd>
        </RowTemplate>
    </MudTable>
}
