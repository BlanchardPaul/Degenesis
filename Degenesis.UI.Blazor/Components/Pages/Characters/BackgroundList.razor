@page "/backgrounds"
@using Degenesis.Shared.DTOs.Characters
@using Degenesis.UI.Service.Features.Characters
@inject BackgroundService BackgroundService

<h3>Backgrounds</h3>

<MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="() => ShowCreateDialog()">Create New Background</MudButton>

@if (backgrounds == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <MudTable Items="@backgrounds" Hover="true" LoadingProgressColor="Color.Info">
        <HeaderContent>
            <MudTh>Name</MudTh>
            <MudTh>Description</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd DataLabel="Name">@context.Name</MudTd>
            <MudTd DataLabel="Description">@context.Description</MudTd>
            <MudTd>
                <MudButton Color="Color.Primary" OnClick="() => ShowEditDialog(context.Id)">Edit</MudButton>
                <MudButton Color="Color.Error" OnClick="() => DeleteBackground(context.Id)">Delete</MudButton>
            </MudTd>
        </RowTemplate>
    </MudTable>
}